import { variants } from "@catppuccin/palette";

export const colors = {
  primary: variants.mocha.maroon.hex,
  secondary: variants.mocha.peach.hex,
  tertiary: variants.mocha.crust.hex,
  quaternary: variants.mocha.base.hex,
  quinary: variants.mocha.yellow.hex,
  disabled: variants.mocha.surface2.hex,
};

export const chartColors = {
  primary: variants.mocha.maroon.hex,
  secondary: variants.mocha.peach.hex,
  tertiary: variants.mocha.red.hex,
};
